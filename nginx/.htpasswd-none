# Placeholder htpasswd file referenced by conf.d/static-site.conf for hosts
# that are NOT behind basic auth (auth_basic is "off" for them, so this file
# is never actually read). Create real .htpasswd files with:
#   openssl passwd -apr1   (or: htpasswd -nb user password)