; PHP security hardening (FPM SAPI). nginx already guards the path-info
; vector with try_files $uri =404; this is defence in depth.
cgi.fix_pathinfo = 0
expose_php = Off
display_errors = Off
log_errors = On